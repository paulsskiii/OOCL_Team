spring:
  application:
    name: cargo-service
    instance: 1
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
server:
  port: 8081
#mvn spring-boot:run '-Dspring-boot.run.arguments=--spring.profiles.active=prod --server.port=8082'