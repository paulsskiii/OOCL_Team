import { useState } from 'react'

interface props {
    id: number;
    title: string;
    year: number;
}

function MyButton({}) {

  return (
    <button>
    </button>
  )
}

export default MyButton
